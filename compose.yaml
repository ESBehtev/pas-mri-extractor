services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKER_IMAGE:-pas-mri-extractor:local}
    ports:
      - "8501:8501"
    env_file:
      - .env
    restart: unless-stopped
    gpus: all
